# Python
venv/
__pycache__/
*.pyc
*.pyo

# Environment
.env
.env.local
firebase-debug.log
.firebase/

# Runtime member data and server credentials
members.sqlite3
members.sqlite3-*
firebase-service-account*.json

# Scraper output
events_output.json

# Social review queue (runtime state)
staged/

# One-shot re-research ledger (runtime state)
recatalog_progress.json

# Discovery runtime state: the URL ledger churns every cycle, and the cache
# holds third-party data files (OpenFlights) that are re-downloadable
discovery/ledger.json
discovery/cache/

# Hero photos and their index: licensed images fetched from Commons/Openverse,
# kept on disk and re-fetchable, never in git (phase 4)
web/media/
media_index.json

# Playwright browsers (downloaded separately)
~/.cache/ms-playwright/

# OS
.DS_Store
Thumbs.db
.output.lock
