# Phase 2 spec — free-first model routing + queue throughput

Plan §4.4, §3.3. Repo /home/azureuser/kidsevents-ie. Builds on phase 1 (`promote()` four LLM steps). Exclusions: no prompt changes beyond passing `kind`; no new lanes of discovery; no web changes except the Production numbers the admin already reads from `factory_state.json`.

## Facts verified 2026-09-13 (do not re-derive)
- Local model: mini PC `llama-server` on 127.0.0.1:8089 (Qwen3.5-35B-A3B, OpenAI-compatible `/v1/chat/completions`, `/health`, `/metrics` with `llamacpp:requests_processing`; 2 slots × 8192 ctx). NOT reachable over the tailnet from the VM; an SSH local forward works (`ssh -N -L 18089:127.0.0.1:8089 mini-pc`, answers in ~2 s). WanderTold's factory and photo gate share it — yield when `requests_processing >= 2`.
- OmniRoute gateway on the VM at http://127.0.0.1:20128 (reverse tunnel from the mini PC): `POST /v1/chat/completions` with NO auth header works for `model: auto/best-free` (default lane; answered "OK"). Named `:free` lanes rot: `openrouter/minimax/minimax-m2.7:free` now returns 404 "unavailable for free"; `openrouter/nvidia/nemotron-3-super-120b-a12b:free` failed (cooldown). A named-lane error must fall through to the next lane, never fail the item. Any `/v1/models` call returns "Invalid API key" — don't use it.
- hermes CLI (`HERMES_BIN`, provider openrouter, gemma-4-31b) works but is the slowest lane (22–90 s) and throttles at ~1 req/min per model.
- Roster of candidate named lanes (probe at start of each run, keep the ones that answer a 5-token test in <30 s): `openrouter/nvidia/nemotron-3-super-120b-a12b:free`, `oc/mimo-v2.5-free`, `kg/tencent/hy3:free`, `kg/stepfun/step-3.7-flash:free`, `kg/meituan/longcat-2.0-free`, `openrouter/dots-studio/dots-3-note-preview:free`, `openrouter/google/gemma-4-31b-it:free`, `oc/nemotron-3-ultra-free`.

## Delta
1. `llm.py` (new): `complete(prompt, kind, max_tokens=1200, timeout=120) -> str`. Order: (a) local via `LOCAL_LLM_URL` (default `http://127.0.0.1:18089`) if `/health` ok and `requests_processing < 2` and `len(prompt) <= 12000`; (b) OmniRoute named lanes from `ROUTING_LANES` (probed once per process, round-robin, a lane that errors/cools down is parked for 10 min); (c) OmniRoute `auto/best-free`; (d) hermes CLI; (e) `""`. Every call appends one line to `routing.jsonl`: `{ts, kind, lane, model, ms, ok, prompt_chars, err}`. `factory_worker.hermes()` becomes a thin wrapper over `complete()` so phase 1's steps need no change.
2. VM user unit `kidsevents-llama-tunnel.service` (`ssh -N -o ServerAliveInterval=30 -o ExitOnForwardFailure=yes -L 18089:127.0.0.1:8089 mini-pc`, `Restart=always`, `RestartSec=15`), enabled; `llm.py` treats the tunnel being down as "local unavailable" (falls through in <2 s). Nothing is installed or changed on the mini PC.
3. Throughput: `staging.py sweep` runs `SWEEP_WORKERS` (default 3) threads, each item still classify-outside-lock / apply-under-lock; per-item budget = 4 calls (phase 1) and a hard `MAX_ITEM_SECS=300`; a `needs_input` verdict never retries automatically; `rejected` never retries. `factory_state.json` gains `llm: {calls_today, by_lane: {lane: {calls, errors, p50_ms}}, last_probe}` for the admin Production view (the existing `/admin/api/status`/`pipeline` routes expose `factory_state.json` — confirm and add the block, no new route).
4. Remove `MAX_LLM_PER_CYCLE` throttling if it exists only because hermes was slow; keep a cycle wall (`CYCLE_WALL_SECS=2400`) so the hourly timer never overlaps itself (`daemon.lock` flock, WanderTold pattern: skip the run with a log line if the previous cycle still holds it).

## Verification (real, in the report)
- `pytest test_llm.py` (lane fallback with a fake HTTP server: local busy → named lane 404 → auto → hermes → "").
- `systemctl --user status kidsevents-llama-tunnel.service` active; `curl 127.0.0.1:18089/health` = ok; one `llm.complete("Reply OK","probe")` answered by `local` per `routing.jsonl`.
- Sweep 30 items (`SWEEP_LIMIT=30 staging.py sweep`, 3 workers) and report: items, approved, still-needs-review with the reasons the 0b gate returned, wall time, calls by lane, p50 per lane, hermes share (target: <10%). (The full backlog is re-swept at the end of phase 1, once the four-step pipeline and `needs_input` semantics exist — phase 2 runs BEFORE phase 1 because the OpenRouter/hermes lane is timing out on every call as of 2026-09-13 17:40 UTC and phase 1 cannot be verified without a working lane.)
- One hourly factory cycle completes within the wall; `routing.jsonl` shows the local lane first.
- Commit + push with the two trailers. Stop.
