You are the implementer for phase 1 of the Small Days overhaul: one record contract, a facet vocabulary with hard validation, the QA gate, and a one-shot migration. Deliver exactly what the spec says, verify with real commands, commit, push, stop.

## Context (carry forward)
- Repo /home/azureuser/kidsevents-ie on this VM, branch main, origin = Forgejo. Live: `kidsevents-ie.service` (Flask 127.0.0.1:8128), `kidsevents-factory.timer` hourly. Venv: `venv/bin/python3`.
- Phase 0b (just merged) added `write_json_atomic`, `load_json_store` (raises on a torn file), `prune_past_events`, `country`/`family_relevant`/`title` fields in the two prompts with gates in `publish_event`/`publish_place`, `confidence` = completeness, `SWEEP_LIMIT` for `staging.py sweep`, `threaded=True` + run-now lock in server.py, `listings` category in sources.json. Read `git log -3 --stat` and those functions before designing.
- `output_lock()` is re-entrant per thread; every store write goes through it and `write_json_atomic`. `staging.try_approve_by_url()` classifies outside the lock and applies under `mark_candidate()`. Keep both shapes.
- Verified facts (do not re-test): Instagram permalink HTML fetched from this VM with a Chrome desktop User-Agent contains `"caption":{"text":…}` and the author; TikTok captions come keyless from `https://www.tiktok.com/oembed?url=…`; the mini PC cannot fetch Instagram captions, so the VM-side fetch is the only caption source for keyword-search candidates.
- The spec is the source of truth: /home/azureuser/kidsevents-ie/tasks/specs/phase1-contract-taxonomy.md (sections 1–7). The plan for background only: tasks/overhaul-plan-2026-09-13.md §3, §4.2.

## Scope lock
- MUST edit only: factory_worker.py, staging.py, server.py, new files contract.py, gate.py, migrate_contract.py, catalog/facets.json, test_contract.py, test_gate.py, test_migrate.py, README.md (contract section), tasks/todo.md (tick phase 1 items). Nothing under web/ (another executor owns web/admin.html; it will read `missing_field` from the staged records you write). 
- MUST NOT edit web/index.html, sources.json, systemd units, anything on the mini PC, or any file outside this repo. MUST NOT change the response shape of the existing `/api/events`, `/api/places`, `/api/holidays`, `/admin/api/*` routes: the old public routes serve `contract.legacy_view()` of on-air records; new full-contract routes live at `/api/v1/*`.
- MUST NOT add dependencies. Stdlib + what the venv already has.
- MUST back up `events_output.json places_output.json holidays_output.json staged/social_candidates.json` to `~/backups/kidsevents-ie/pre-phase1-<UTC stamp>.tgz` before the migration runs, and the migration MUST be idempotent (`schema_version: 1` marker).
- Stop and report (do not proceed) if: the migration would drop more than 90% of on-air events; any existing test in the repo fails before your changes; the LLM lane returns "" for all three end-to-end candidates (then report the routing.jsonl / log evidence instead of retrying more than twice).

## Target state
1. `catalog/facets.json` exactly as spec §3 (32 counties with region and NI flag, legacy maps, holiday facets with the 2026/27 school-break dates).
2. `contract.py`: `KINDS`, `EMPTY_RECORD(kind)`, `validate(record) -> list[str]`, `completeness(record) -> float`, `legacy_view(record) -> dict` (keys listed in spec §1), `slugify`, `maps_url(location)` (google.com/maps/search/?api=1&query=…, never a Places API), `region_for(county)`.
3. `gate.py`: `gate_meta(taxonomy) -> (clean, dropped)` (alias map, then drop unknown values, never default), `qa(record, sources_text) -> (ok, reason, missing_field)` implementing spec §2 "required to be on-air" and plan §4.2 (family relevance, past date, `date_evidence` quote present in sources_text, Ireland flags, title ≠ caption, description length, duplicate fold against on-air titles, place needs an activity type, holiday needs 2 source domains). Facet drop counts accumulate in `factory_state.json["facet_drops"]`.
4. LLM steps (spec §4 + §4b): `research_fetch(candidate)` (VM-side fetch with Chrome UA, TikTok oEmbed, 1 req/2 s per platform, 429/403 → needs-input `caption`), then `classify`, `facts` (quotes verified as substrings; non-matching facts dropped), `extract` (kind-specific; `date_evidence` must be one of the fact quotes), `write` (title ≤80 never the caption, summary ≤160, description with the word bands, taxonomy from `facet_gloss()` and validated by `gate_meta`). `promote(candidate) -> (record|None, reason, missing_field)`; `promote_candidate` keeps its 3-tuple for staging.py (kind, record, reason) and staging stores `missing_field` and uses status `needs_input` vs `rejected` per spec §5. `run_discovery_cycle` per-page extraction uses the same steps; JSON-LD events pre-fill `event.*` and skip `extract`. At most four `hermes()` calls per candidate.
5. `migrate_contract.py` run once: table of kept / needs-input / rejected per file with top-5 reasons; needs-input records move to `staged/needs_input.json` (same lock) and out of the public files; the three public files hold only on-air contract records.
6. server.py: old routes → `legacy_view`; `/api/v1/{events,places,holidays}` → full records; `/admin/api/*` → full records (admin.html reads `reason`/`hint` already; add the `missing_field` line).
7. Tests: `test_contract.py`, `test_gate.py`, `test_migrate.py` (fixture records: one good event, one past event, one adult event, one caption-titled candidate, one place, one holiday with 1 vs 2 domains). One assertion per test; no network, no LLM.

## Evidence required in the final report (≤60 lines, file:function references)
- `venv/bin/python3 -m pytest -q` output (all test files in the repo green) and `py_compile` of every module.
- The migration table and the backup path.
- `curl -s 127.0.0.1:8128/api/events | python3 -c '…'` showing count + a sample record with the legacy keys; `curl …/api/v1/events` showing one contract record; `/api/holidays` and `/api/v1/holidays` empty lists.
- After `systemctl --user restart kidsevents-ie.service`: a Playwright load of `http://127.0.0.1:8128/` at 1400×900 with zero console errors and a screenshot path (venv has playwright + chromium).
- Three real end-to-end `promote()` runs on staged candidates whose `found_via` contains `corkfamily` or `galwayfamily` and that have a caption or an Instagram permalink: the research fetch result (caption found? chars), the four call outcomes, and either the record (title, kind, county, taxonomy) or `reason` + `missing_field`.
- `git log --oneline -1` after `git add -A && git commit` (message lists the delta; trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01BsEd8XKU1NMdhWwev3Xnpw`) and `git push origin main` (`git pull --rebase --autostash origin main` first if rejected).
- Anything from the spec you did not do, with the reason. Only make changes directly requested.
